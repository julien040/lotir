export default tableProps;
declare namespace tableProps {
    namespace baseStyle {
        namespace table {
            const backgroundColor: string;
            const borderRadius: number;
        }
    }
    const sizes: {};
    const variants: {};
    namespace defaultProps {
        const size: string;
        const variant: string;
    }
}
//# sourceMappingURL=table.d.ts.map